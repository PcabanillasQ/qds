export interface Product {
  createdAt: string;
  name: string;
  image: string;
  price: string;
  description: string;
  id: string;
  color: string;
  material: string;
  tag: string;
  addedToCart?: boolean;
}
